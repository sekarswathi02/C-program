#include <stdio.h>
void main(){
    int num;
    scanf("%d",&num);
    int count=0;
    for(int i=2;i<=num/2;i++)
    {
        if(num%i==0)
        count++;
    }
    if(count==2)
    printf("Prime");
    else
    printf("Not a prime");
    
}
