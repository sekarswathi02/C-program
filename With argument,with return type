#include <stdio.h>
float area(int);
void main()
{
    int r;
    float a;
    scanf("%d",&r);
    a=area(r);
    printf("Area is:%f",a);
}
float area(int r)
{
    float area=3.14*r*r;
    return area;
}
